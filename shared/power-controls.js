import {DAYLIGHT_MINUTES,LOW_BATTERY} from './power.js';
const hours = () => [...Array(24).keys()].map(h => `<option value="${h}">${String(h).padStart(2, '0')}:00</option>`).join('');
// Power and motion (shared/power.js), for the workshop and the phone page.
export function powerControls(root, getSettings, onChange) {
  root.innerHTML = `<div class="power-controls"><label class="toggle"><span>Minute change<small>The figures reveal each new minute over 400 ms.</small></span><input type="checkbox" data-minute-animation aria-label="Minute change"></label><label class="toggle"><span>Pulse &amp; swipes<small>The marker pulse, the tray swipe and the clock’s glide.</small></span><input type="checkbox" data-flourishes aria-label="Pulse and swipes"></label><label class="field">Stop animations at<select data-low-battery aria-label="Stop animations at">${LOW_BATTERY.map(b => `<option value="${b}">${b}% battery</option>`).join('')}</select></label><label class="field">Daylight updates<select data-daylight aria-label="Daylight updates">${DAYLIGHT_MINUTES.map(m => `<option value="${m}">Every ${m} minutes</option>`).join('')}</select></label><p class="micro">How often the watch reshades the map for the moving sun. Longer saves battery; the day–night line moves about a pixel every five minutes.</p><label class="toggle"><span>Night saver<small>At night the map reshades every other hour and nothing animates.</small></span><input type="checkbox" data-night aria-label="Night saver"></label><div class="night-hours"><label class="field">From<select data-night-start aria-label="Night saver from">${hours()}</select></label><label class="field">Until<select data-night-end aria-label="Night saver until">${hours()}</select></label></div><label class="toggle"><span>During Quiet Time<small>Also when Quiet Time is on on the watch, whatever the hour.</small></span><input type="checkbox" data-quiet-time aria-label="During Quiet Time"></label><label class="toggle"><span>Pause in the dark<small>At night, redraw only while the backlight is on: the screen keeps its last picture until the light comes on, then catches up at once.</small></span><input type="checkbox" data-dark-pause aria-label="Pause in the dark"></label></div>`;
  const $ = s => root.querySelector(s), minute = $('[data-minute-animation]'), flourishes = $('[data-flourishes]'), daylight = $('[data-daylight]'),
    night = $('[data-night]'), start = $('[data-night-start]'), end = $('[data-night-end]'), dark = $('[data-dark-pause]'), quietTime = $('[data-quiet-time]'), low = $('[data-low-battery]');
  const commit = () => { onChange({daylightMinutes: Number(daylight.value), minuteAnimation: minute.checked, flourishes: flourishes.checked, night: night.checked, nightStart: Number(start.value), nightEnd: Number(end.value), quietTime: quietTime.checked, darkPause: dark.checked, lowBattery: Number(low.value)}); refresh(); };
  for (const input of [minute, flourishes, low, daylight, night, start, end, quietTime, dark]) input.onchange = commit;
  function refresh() {
    const s = getSettings(), p = s.power;
    minute.checked = p.minuteAnimation; flourishes.checked = p.flourishes; daylight.value = String(p.daylightMinutes);
    night.checked = p.night; start.value = String(p.nightStart); end.value = String(p.nightEnd); dark.checked = p.darkPause; quietTime.checked = p.quietTime; low.value = String(p.lowBattery);
    minute.disabled = flourishes.disabled = low.disabled = !s.motion; start.disabled = end.disabled = !p.night; dark.disabled = !p.night && !p.quietTime;
  }
  refresh(); return {refresh};
}
